name: my first github action
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container: 
        image: fredoo404/build
    steps:
        - name: Fix
          run: sudo chown -R circleci /github && sudo chmod -R 777 /github
        - uses: actions/checkout@master
        - name: Test ls 1
          run: ls -l 
        - name: Hello world
          run: python ./main.py